const estoque = ["monitor", "teclado", "mouse", "placa-mãe", "CPU"];

estoque.push("placa de video");
estoque.shift(estoque[0]);

console.log(estoque[0]);
console.log(estoque[1]);
console.log(estoque[2]);
console.log(estoque[3]);
console.log(estoque[4]);  

let clienteNome = "nicoly";
let clienteIdade = 16;
let possuiCupom = true

function processarValor (valorTotal, cupomAtivo, quantidadeItens){
    var ver
}

